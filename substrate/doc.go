// Package substrate is the node mediator. It wires all node-local services:
// Mirage (space isolation), Lector (file+symbol understanding), Troupe (EventLog),
// Vessels (agent harnesses), and the Hierophant connection (context enrichment).
// Was: miraged (dissolved for SRP).
package substrate
